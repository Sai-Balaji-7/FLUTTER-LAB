import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
      home: Scaffold(
    backgroundColor: Colors.purple.shade100,
    appBar: AppBar(title: Text("MY FIRST APPLICATION")),
    body: Center(
        child: Container(
            width: 200,
            height: 300,
            color: Colors.deepPurpleAccent,
            alignment: Alignment.center,
            child:
                Column(mainAxisAlignment: MainAxisAlignment.center, children: [
              Image.network(
                "https://static.wikia.nocookie.net/gachiakuta/images/3/3e/Watchman_Series_Logo.png/revision/latest/scale-to-width-down/1200?cb=20240527045521",
                width: 50,
                height: 50,
              ),
              Icon(
                Icons.school,
                color: Colors.lightGreenAccent,
                size: 50,
              ),
              SizedBox(height: 10),
              Text(
                "Hello Flutter",
                style: TextStyle(
                    fontSize: 30,
                    fontWeight: FontWeight.bold,
                    fontStyle: FontStyle.italic),
              )
            ]))),
  )));
}
